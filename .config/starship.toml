# Starship prompt configuration
# Managed by chezmoi - edit with: chezmoi edit ~/.config/starship.toml
# Documentation: https://starship.rs/config/

# Timeout for starship to execute commands (in milliseconds)
command_timeout = 1000

# Format of the prompt
format = """
[╭─](bold green)$username$hostname$directory$git_branch$git_status$git_state$docker_context$nodejs$python$rust$golang$package
[╰─](bold green)$character"""

# Alternative: Single line format
# format = "$all"

[character]
success_symbol = "[➜](bold green)"
error_symbol = "[✗](bold red)"
vicmd_symbol = "[V](bold green)"

[username]
style_user = "bold cyan"
style_root = "bold red"
format = "[$user]($style)"
disabled = false
show_always = false

[hostname]
ssh_only = true
format = "[@$hostname](bold yellow) "
disabled = false

[directory]
truncation_length = 3
truncate_to_repo = true
format = " [$path]($style)[$read_only]($read_only_style) "
style = "bold blue"
read_only = " 🔒"
read_only_style = "red"
truncation_symbol = "…/"

[directory.substitutions]
"Documents" = "📄 "
"Downloads" = "📥 "
"Music" = "🎵 "
"Pictures" = "📷 "
"Developer" = "💻 "
"Code" = "💻 "

[git_branch]
symbol = " "
format = "[$symbol$branch(:$remote_branch)]($style) "
style = "bold purple"

[git_status]
format = '([\[$all_status$ahead_behind\]]($style) )'
style = "bold red"
conflicted = "🏳"
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
up_to_date = ""
untracked = "🤷"
stashed = "📦"
modified = "📝"
staged = '[++\($count\)](green)'
renamed = "👅"
deleted = "🗑"

[git_state]
format = '\([$state( $progress_current/$progress_total)]($style)\) '
style = "bright-black"

[docker_context]
symbol = "🐳 "
format = "[$symbol$context]($style) "
style = "blue bold"
only_with_files = true
detect_files = ["docker-compose.yml", "docker-compose.yaml", "Dockerfile"]
detect_folders = []
disabled = false

[nodejs]
symbol = " "
format = "[$symbol($version )]($style)"
style = "bold green"
detect_files = ["package.json", ".node-version"]
detect_folders = ["node_modules"]

[python]
symbol = " "
format = '[${symbol}${pyenv_prefix}(${version} )(\($virtualenv\) )]($style)'
style = "yellow bold"
pyenv_version_name = false
detect_extensions = ["py"]
detect_files = [".python-version", "Pipfile", "pyproject.toml", "requirements.txt", "setup.py", "tox.ini"]
detect_folders = []

[rust]
symbol = " "
format = "[$symbol($version )]($style)"
style = "bold red"
detect_files = ["Cargo.toml"]
detect_folders = []

[golang]
symbol = " "
format = "[$symbol($version )]($style)"
style = "bold cyan"
detect_files = ["go.mod", "go.sum", "glide.yaml", "Gopkg.yml", "Gopkg.lock", ".go-version"]
detect_folders = ["Godeps"]

[package]
symbol = "📦 "
format = "[$symbol$version]($style) "
style = "208 bold"
display_private = false
disabled = false

[cmd_duration]
min_time = 2_000
format = "took [$duration]($style) "
style = "bold yellow"
show_milliseconds = false
disabled = false

[time]
disabled = true
format = '🕙[\[ $time \]]($style) '
time_format = "%T"
style = "bold white"

[battery]
full_symbol = "🔋"
charging_symbol = "⚡️"
discharging_symbol = "💀"
disabled = true

[[battery.display]]
threshold = 10
style = "bold red"

[[battery.display]]
threshold = 30
style = "bold yellow"

[memory_usage]
disabled = true
threshold = -1
symbol = "🐏 "
format = "$symbol[$ram( | $swap)]($style) "
style = "bold dimmed white"

[aws]
format = '[$symbol($profile )(\($region\) )]($style)'
style = "bold yellow"
symbol = "☁️  "
disabled = true

[gcloud]
format = '[$symbol$account(@$domain)(\($region\))]($style) '
style = "bold blue"
symbol = "☁️  "
disabled = true

[kubernetes]
format = '[$symbol$context( \($namespace\))]($style) '
style = "cyan bold"
symbol = "⎈ "
disabled = true

[terraform]
format = "[$symbol$workspace]($style) "
style = "bold 105"
symbol = "💠 "
disabled = true
